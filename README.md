# Fa9lh — Full Front-End (CRA + Tailwind)
Ready-to-run front-end matching the provided UI mockups.

## Run
1. `npm install`
2. `npm start` -> http://localhost:3000

## Notes
- Static UI with light interactivity for demos (quiz selection, progress visuals).
